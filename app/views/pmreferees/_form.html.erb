<%= form_with(model: pmreferee, local: true) do |form| %>
  <% if pmreferee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pmreferee.errors.count, "error") %> prohibited this pmreferee from being saved:</h2>

      <ul>
      <% pmreferee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :pmreferee_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :pmreferee_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
