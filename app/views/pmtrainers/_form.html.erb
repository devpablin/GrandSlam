<%= form_with(model: pmtrainer, local: true) do |form| %>
  <% if pmtrainer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pmtrainer.errors.count, "error") %> prohibited this pmtrainer from being saved:</h2>

      <ul>
      <% pmtrainer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :pmtrainer_nombre %>
  </div>

  <div class="field">
    <%= form.label :apellido %>
    <%= form.text_field :apellido, id: :pmtrainer_apellido %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-success"%>
  </div>
<% end %>
